# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.build/

# Swift Package Manager
.swiftpm/

# macOS
.DS_Store

# Python sidecar is its own independent git repo — don't track its contents here
# (leading slash + exact case needed: macOS's case-insensitive FS would otherwise
# also match WhisperClone/Sidecar/, the Swift sidecar-client code)
/sidecar/
